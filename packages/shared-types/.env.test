# Test Database Configuration
# Use a separate database for testing to avoid conflicts with development data
TEST_DATABASE_URL="postgresql://postgres:password@localhost:5432/stack_test?schema=public"

# Alternative: Use the same database as development (not recommended for production)
# DATABASE_URL="postgresql://username:password@cluster-endpoint:5432/database_name?schema=public"
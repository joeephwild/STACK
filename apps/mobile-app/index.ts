// Import polyfills first
import 'react-native-get-random-values';

// Note: thirdweb v5 doesn't require a separate React Native adapter
// The polyfills are handled by the metro configuration
import "react-native-reanimated";
import "expo-router/entry";
